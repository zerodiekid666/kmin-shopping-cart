import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#d9dc11"
    },
    secondary: {
      main: "#000"
    }
  }
  // color:{
  //   primary: 'black',
  // }
});

export default theme;
