import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#212121"
    },
    secondary: {
      main: "#000"
    }
  }
  // color:{
  //   primary: 'black',
  // }
});

export default theme;
